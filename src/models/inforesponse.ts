/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type InfoResponse = {
  /**
   * The server version.
   */
  version: string;
  /**
   * the features that the server supports.
   */
  features: Array<string>;
};

/** @internal */
export const InfoResponse$inboundSchema: z.ZodType<
  InfoResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  version: z.string(),
  features: z.array(z.string()),
});

export function infoResponseFromJSON(
  jsonString: string,
): SafeParseResult<InfoResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InfoResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InfoResponse' from JSON`,
  );
}
